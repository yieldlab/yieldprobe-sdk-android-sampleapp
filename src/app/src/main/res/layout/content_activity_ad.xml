<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:ads="http://schemas.android.com/apk/lib/com.adition.android.sdk"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:id="@+id/linearLayoutAd"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context=".ActivityAd"
    tools:showIn="@layout/activity_ad">

    <ImageView
        android:src="@drawable/adition_logo"
        android:background="#ffffff"
        android:layout_width="match_parent"
        android:scaleType="fitStart"
        android:layout_height="60dp">
    </ImageView>

    <TextView
        android:id="@+id/txtAdViewSetData"
        android:padding="3dp"
        android:textSize="16sp"
        android:textColor="@color/colorText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Targeting Information">
    </TextView>

    <TextView
        android:id="@+id/txtTargeting"
        android:padding="3dp"
        android:textSize="16sp"
        android:textColor="@color/colorText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Targeting Information">
    </TextView>

    <Button
        android:id="@+id/btnBack"
        android:padding="3dp"
        android:textSize="16sp"
        android:textColor="@color/colorText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Back"></Button>

</LinearLayout>